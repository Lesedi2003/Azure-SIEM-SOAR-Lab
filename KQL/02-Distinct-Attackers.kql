SecurityEvent
| where EventID == 4625
| distinct Account

Returns every unique username that attackers attempted to authenticate with.
This removes duplicate entries generated by repeated brute-force attempts.
